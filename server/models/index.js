// Export core models for the simplified application
const User = require('./User');
const Scheme = require('./Scheme');
const DocumentChunk = require('./DocumentChunk');
const AnalyticsSummary = require('./AnalyticsSummary');

module.exports = {
  User,
  Scheme,
  DocumentChunk,
  AnalyticsSummary
};